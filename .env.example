GROQ_API_KEY=
LLM_PROVIDER=
GROQ_BASE_URL=

# Verify these IDs against Groq's live model list before relying on them.
ANALYSIS_MODEL=
FILTER_MODEL=

MAX_PAGES=
CHAR_BUDGET=
PER_PAGE_CHAR_CAP=

REQUEST_TIMEOUT=
MAX_CONCURRENT_LEADS=

# Free key from https://brave.com/search/api/ (Data for AI - free plan).
# Without it, name->URL search falls back to scraping DuckDuckGo, which gets
# bot-blocked from datacenter IPs.
BRAVE_API_KEY=

DB_PATH=
CACHE_PATH=

CORS_ORIGINS=
